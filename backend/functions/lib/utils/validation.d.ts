/**
 * Validates if a string is a valid Firebase UID format
 */
export declare function isValidFirebaseUID(uid: string): boolean;
/**
 * Validates if an amount is valid for Stripe (must be positive integer)
 */
export declare function isValidAmount(amount: number): boolean;
/**
 * Validates if a currency code is valid
 */
export declare function isValidCurrency(currency: string): boolean;
/**
 * Validates if a platform value is supported
 */
export declare function isValidPlatform(platform: string): boolean;
//# sourceMappingURL=validation.d.ts.map